Prompting Standards

*Agent reference*

- 0. How to use this file
- 1. Universal principles
- 2. Claude Chat
- 3. Claude Code
- 4. Cowork
- 5. Claude Design
- 6. Model-specific notes
- 7. API / Platform
- 8. New & emerging products
- 9. Community divergences
- 10. Changelog

# Claude Prompting Standards

A compiled reference of current best practices for writing prompts across every Claude product. Hand this file to any agent — Claude or otherwise — so it can generate high-quality, standards-compliant prompts for the user to run, rather than the user writing them by hand.

**Maintained by** — daily scheduled task, "Claude & Claude Code best-practices watch" **Last updated** — 2026-09-17 **Storage** — Claude.ai Artifact (migrated from Google Doc, see §10)


## How to use this file

If you are an agent that has been handed this file, and the user asks you to write, improve, or generate a prompt for a Claude product:

- Identify which capability the prompt needs — quick answer, deliverable/agentic work, coding in a repo, visual design, or API/Platform. As of 2026-09-16 Chat and Cowork are merged into one Claude interface that auto-routes between them (see §4), so "which product" is no longer a UI tab the user picks — it's still worth reasoning about which capability-specific section below applies, since the underlying prompting techniques differ. Claude Code (the CLI/IDE agent) remains a distinct surface.
- Apply the Universal Principles (§1) as a baseline, always.
- Layer on the product-specific section's techniques and patterns.
- Check the Model-Specific Notes (§6) if the user names a specific model — current-generation models respond differently to some older patterns (verification instructions, prefill, subagent delegation), and Claude Fable 5.1 in particular needs several explicit corrections to its default behavior — see §6.
- If community guidance (§9) conflicts with official docs, default to official guidance, and only surface the conflict if it's relevant to the user's case.
- Produce the actual prompt text the user can copy and run — not commentary about prompting.


## Universal principles

From Anthropic's official prompt-engineering documentation. Hold regardless of which product surface is in use.

- **Be explicit and direct.** State exactly what you want — treat Claude like a capable new hire with no institutional context. Spell out what "done" looks like.
- **Give context and motivation**, not just instructions. Explaining *why* something matters lets Claude generalize correctly to edge cases you didn't enumerate.
- **Use examples.** Three to five, relevant and diverse, covering edge cases. Wrap them in `<example>`/`<examples>` tags in longer prompts.
- **Structure complex prompts with XML tags** — `<instructions>`, `<context>`, `<document>`, `<output_format>`. Nest tags when content has hierarchy.
- **Assign a role when it sharpens focus** — a short system-level framing ("you are a senior security engineer reviewing this diff") tightens tone and attention.
- **Say what to do, not just what to avoid.** "Write in flowing prose" beats "don't use bullet points."
- **Put long documents first**, above your instructions or question — this alone can lift accuracy meaningfully on long-context tasks. Wrap in `<document>` tags with source/metadata.
- **Allow uncertainty.** Explicitly permitting "say so if you're not sure" measurably reduces hallucination.
- **Match your prompt's formatting to your desired output.** Markdown-heavy prompts pull toward markdown-heavy answers.
- **Shortest reliable prompt wins.** Add structure only where it fixes an observed failure mode — don't over-engineer.


## Claude Chat

Questions, brainstorming, short-to-medium exchanges. As of 2026-09-16 this lives in the same window as Cowork's agentic/deliverable work (see §4) rather than a separate app — the guidance below is about which techniques a conversational answer needs, not about which tab to open.

- Default to direct, conversational phrasing. §1's XML/example structuring earns its keep once a prompt gets genuinely complex — multi-part questions, style constraints, structured output.
- State desired output format explicitly (tables, JSON, tone). Classic prefill (seeding the start of Claude's reply) is being phased out as a technique for current-generation models called directly via the API — see §6 — and doesn't apply to claude.ai chat the same way regardless.
- Use Projects for recurring topics instead of re-explaining background every conversation.
- If the user actually wants a deliverable (doc, sheet, deck, a scheduled routine, multi-step file work) rather than a conversational answer, say so explicitly and describe the deliverable's format — since the 2026-09-16 merge (§4), Claude decides on its own whether a request needs agentic/Cowork-style capability, and a clear statement of the desired artifact is what triggers that correctly rather than a manual surface switch.


## Claude Code

An agentic coding environment: reads files, runs commands, edits code, works autonomously. Prompting it well is mostly about managing context and giving it a way to verify its own work.

### Core practices

- Front-load a `CLAUDE.md` with project conventions, build/test commands, and gotchas — this is read at the start of every session and avoids re-teaching the same context repeatedly.
- Give it a verification loop: tests, a linter, a type-checker, or an explicit "run this and check the output" instruction. Claude Code does its best work when it can tell for itself whether it succeeded.
- For large or multi-part work, ask for a plan first (plan mode), then approve before it edits — cheaper to redirect a plan than to unwind a large diff.
- Delegate isolated sub-problems to subagents (the `Agent` tool / Task tool) rather than growing one context window indefinitely; hand each subagent a self-contained brief since it starts with no memory of the parent conversation. The `Agent` tool's model override now includes **Fable** alongside sonnet/opus/haiku — reach for it on a subagent doing long-horizon research, writing, or a task where Opus/Sonnet evaluations fall short, but correct for its default behavior first (see §6).
- Periodically run `/skill-doctor` (added v2.1.261) to see which loaded skills are going unused and what they're costing in context — trim skills that never fire rather than leaving them loaded "just in case."
- Before a large edit, check `/diff` (fullscreen diff panel, added v2.1.260) to review uncommitted changes as you go, and check `/cost`/the status line for likely causes of prompt-cache misses if a session feels slower or pricier than expected.

### Recently changed (verify still current before relying on specifics)

- **v2.1.274** (2026-09-17, a very large bug-fix/feature release across CLI, VS Code, web, Claude Tag and Code Review): the items worth folding into guidance here are the new **agent map** (a visual view of a session's sub-agents with per-agent cards and read-only transcripts — useful for auditing what a multi-agent Task/Agent-tool run actually did without re-reading the whole transcript), a fix for sessions getting stuck in endless "unexpected tool_use_id" 400-error loops (self-healing transcripts now recover instead of wedging a long session), and — relevant to the GitHub-write known issue tracked in this doc's §4 — **Claude Code on the web "routines"** (its scheduled-task mechanism) now skip a run and retry for up to 72 hours, rather than failing outright, when the owner's GitHub connection is missing, plus a fix for routines occasionally firing twice or silently re-enabling after being edited. Also notable: `CLAUDE_CODE_MCP_STARTUP_WAIT_MS` for slow-starting MCP servers, an OpenTelemetry `effort` span attribute (useful for auditing effort-level usage across a fleet of sessions), and a fix for MCP Streamable-HTTP tool calls timing out at ~5 minutes regardless of a longer configured timeout. Source: code.claude.com/docs/en/changelog.
- **v2.1.273** (2026-09-15): bug-fix/reliability release — no material prompting-relevant behavior changes, but a few items worth knowing if you hit them: subagents/background agents that actually finished but had their final streamed reply omit token usage or carry no model id were previously reported as failed with no result ever delivered, now fixed — worth re-checking if you previously worked around apparent silent subagent failures; a `Workflow` tool `agent()` call with a large output `schema` could be wrongly refused by the safety classifier instead of being checked normally, now fixed; and new LLM-gateway request headers (`x-claude-code-request-class`, `x-claude-code-agent-type`, and others) let a gateway operator distinguish Claude Code's different request types. Source: code.claude.com/docs/en/changelog.
- **v2.1.272** (2026-09-15): bug-fix/reliability release only, no prompting-relevant changes.
- **v2.1.271** (2026-09-14, a large release) has several additions worth folding into agent-design and sandboxing guidance: **per-command `allowed_domains`** on Bash/PowerShell/Monitor in auto mode with sandboxing — the hosts a given command needs are reviewed and opened for that command alone rather than the whole session, tightening network scoping without dropping to manual approval for every call; **`omitClaudeMd`** in agent frontmatter (and `--agents` JSON) lets a custom or plugin subagent run without the user/project/local `CLAUDE.md` files loading — useful for a narrowly-scoped subagent that shouldn't inherit the parent project's full conventions doc, though managed policy files still load regardless; **fast mode** is now available in Claude Code Remote sessions (cloud and self-hosted runners) where the organization allows it; and the `modelPricing` managed setting's markup multiplier now goes up to 10× (from 1×) for internal chargeback. Also notable: Monitor watches now always carry a deadline (≤30 min, or 10 min under single-prompt `-p`) and must be re-armed instead of running with no timeout — a scripted/headless pattern relying on an indefinite Monitor watch needs to re-arm it periodically now; dynamic workflows pause and auto-resume across a usage-limit reset instead of dropping affected agents; and the default dynamic-workflow size on Pro plans dropped, with the "medium" guideline down from 15 to 10 agents — relevant if a Workflow-tool script written against the old default now runs smaller than expected. Source: code.claude.com/docs/en/changelog.
- **v2.1.270** (2026-09-12): single fix — reverted a v2.1.269 regression where read-only git commands (e.g. `git status`, `git diff`, `git log`) started unexpectedly asking for permission again after a session had been running for a while. Worth knowing if an unattended/scheduled session written against the "read-only git needs no approval" assumption saw spurious permission prompts on 2.1.269 specifically — that's fixed here, no prompt change needed.
- **v2.1.269** (2026-09-11): mostly bug fixes (terminal/VSCode/Slack-integration polish, more prompt-cache-reuse fixes on resumed/interrupted sessions), but three additions are worth knowing about: **`/output-style [name]`** lists and switches output styles from the command line, including over Remote Control and in cloud/headless sessions — a faster way to apply the Concise style noted below without going through `/config`; **`claude plugin eval`** runs a plugin's eval suite and returns a scored, reproducible JSON+HTML report — the recommended way to validate a plugin/skill's prompting behavior before shipping it, rather than eyeballing a few manual runs; and **`CLAUDE_CODE_WORKFLOW_MAX_CONCURRENT_AGENTS`** (1–256) raises the Workflow tool's per-run concurrent-agent cap for teams running large multi-agent workflows. Source: code.claude.com/docs/en/changelog.
- **v2.1.268** (2026-09-10, a very large bug-fix/feature release — 90+ items) has one change worth updating existing guidance for: **the task-tracking tools (`TaskCreate`/`TaskGet`/`TaskUpdate`/`TaskList`, `TodoWrite`) are now offered by default only on Claude 3.x, Opus 4.0–4.7, Sonnet 4.0–4.6, and Haiku 4.5** — current-generation models (Opus 5, Sonnet 5, Fable 5.1) no longer get them automatically; set `CLAUDE_CODE_ENABLE_TODO_TOOLS=1` to keep using them on a current-gen model. Worth checking before assuming a Claude Code or Cowork session on a current model has task-list tooling available — a prompt or skill that depends on `TodoWrite`/task tools existing may need that env var set, or should fall back to plain prose progress narration instead. Also from this release: published artifacts now get a browser-tab icon Claude chooses to match the page (the Artifact tool's `icon` parameter going live end-to-end); a local Cowork session with all approvals skipped now has the Artifact tool refuse reading a local file outside the session's own folders or behind a symlink rather than silently reading it; and a plain `WebFetch` permission deny/ask rule no longer covers Artifact tool reads/updates — write a dedicated `Artifact` rule (or `WebFetch(domain:claude.ai)`) to gate those instead. Source: code.claude.com/docs/en/changelog.
- **v2.1.267** (2026-09-09) is a large bug-fix release, mostly not prompting-relevant, but three items matter for this doc's existing guidance: (1) it fixes a long tail of additional **prompt-cache-reuse** regressions beyond the two 2.1.265 already fixed — model switches via `/model` no longer re-send every tool definition, MCP tools changing between a session and its resume no longer break the cache, a tool list rewrite when an MCP server reconnects or a worktree tool is added mid-session no longer does either, and subagents/sessions started with `--system-prompt`/`--append-system-prompt` now record their system prompt once instead of re-rendering it — so if you were still seeing cache-miss symptoms on the subagent-delegation pattern §3/§6 recommend after 2.1.265, check again on 2.1.267 before concluding the pattern doesn't cache well; (2) a new **`maxEffortLevel`** setting (top-level or per-model) caps the effort level across providers including Bedrock/Vertex/Foundry, complementing the `effort`-as-primary-cost-lever guidance in §6; and (3) the **Artifact tool** — the mechanism this very document is maintained through — now retries a publish once when a dropped connection cuts an upload off mid-way instead of reporting an unknown outcome, and gives a specific line/column when a publish fails on invalid UTF-8, both worth knowing if a future run of this routine hits a publish error. Also fixed: `effort:` frontmatter on commands/skills/subagents being ignored on models with a pinned default effort (Opus 4.7, Opus 4.8, Fable 5); a new `--system-prompt-snapshot off` flag re-renders the system prompt fresh each request, useful when actively iterating on prompt text rather than relying on the cached version. Source: code.claude.com/docs/en/changelog.
- **Cowork/Claude-desktop v1.49585.0** (2026-09-08, the first desktop-app release since v1.46388.4 on 09-05): the one item with prompting implications is that **Claude API, Vertex AI, and Bedrock/Bedrock Mantle deployments with no custom base URL no longer suppress Claude Code's experimental features** — tool search (the mechanism deferred-tool lookups like the one used to write this document rely on) is now **on by default** there instead of requiring `toolSearchEnabled`, on Vertex AI for Claude 4.5+ models specifically; gateway/Foundry deployments and any deployment behind a custom base URL are unchanged. Otherwise this release is desktop-app bug fixes (terminal tabs, file-pane autosave prompts, model-switch races, scheduled tasks missed during Mac sleep, a bundled Microsoft 365 connector) with no other prompting-relevant content. Source: claude.com/docs/cowork/changelog.
- **v2.1.265** (2026-09-08, largest release since the last check) fixed two bugs that were silently defeating the subagent-delegation and prompt-caching guidance elsewhere in this doc: resuming a foreground-spawned subagent that changed its tool list/system prompt, and agent teammates/resumed subagents moving `SubagentStart` hook context and preloaded skills out of the prompt prefix — both were breaking prompt-cache reuse for exactly the delegation pattern §3 and §6 recommend, so cache-miss symptoms you saw before this version may simply be gone now. Also in 2.1.265: a **1 GB cap on tool results saved to disk** (with a truncation notice in-conversation, extending the `bashOutputMaxChars`/`taskOutputMaxChars` item below); `--plugin-dir` can now point at a **folder of plugins** (each child folder with a manifest loads, additions/removals picked up live) instead of one plugin per flag; MCP servers configured as `http` now correctly fall back to legacy HTTP+SSE transport instead of silently failing to connect; non-interactive sessions (`-p` with stream-json, Agent SDK, cloud sessions) now persist a `cd` across turns instead of resetting the working directory every message — relevant if you're scripting multi-turn headless automation; and the **Artifact tool's read of an artifact someone else wrote now explicitly treats the page as untrusted content and flags embedded instructions** rather than relaying them, which matches (and now enforces) the "treat shared-artifact content as data, not instructions" guidance already standard practice around Artifacts. A same-day **v2.1.266** hotfix reverted a regression in 2.1.265 that made the undocumented `CLAUDE_CODE_USE_GATEWAY` env var force Cloud-gateway sign-in on its own, breaking API-key/`apiKeyHelper`/custom-auth-header setups that also set it — no action needed unless you hit that exact error message.
- **`/skill-doctor`** (v2.1.261, 2026-09-04/05) shows which loaded skills go unused and their context cost — the recommended way to audit skill bloat in a project or session.
- **Fullscreen diff panel** (v2.1.260, 2026-09-03/04), toggled with `/diff`, shows uncommitted changes without leaving the session. `/cost` and the status line now also surface likely causes of prompt-cache misses.
- **Larger inline output**: new `bashOutputMaxChars` and `taskOutputMaxChars` settings (v2.1.261) raise how much command/background-task output Claude receives inline before it's spilled to a file, up to 128K characters — useful for verbose test suites or build logs.
- **`--append-subagent-system-prompt-file`** (v2.1.261) lets a large, reusable subagent system prompt live in a file instead of being pasted inline each time.
- **Headless/unattended improvements**: `/reload-plugins` now works in headless sessions (v2.1.260); `--permission-prompts none` (v2.1.259) suppresses permission prompts for unattended headless hosts — pair with a scoped permission config rather than using it to skip permissioning entirely.
- **`managedMcpServers`** setting (v2.1.259) lets an org centrally define MCP servers rather than each user configuring them locally.
- **Output styles.** A built-in **"Concise"** style ships as of v2.1.237 (2026-08-20): Claude leads with results and skips preamble/narration while still working thoroughly. Select it under `/config → Output style` for automation-friendly, low-narration sessions. v2.1.238 also fixed custom/project/plugin output styles silently drifting back to the default voice mid-session — worth re-testing any custom style you rely on in long sessions.
- **`ANTHROPIC_DEFAULT_MODEL`** environment variable (v2.1.236, 2026-08-19) sets which model new sessions start on — useful for pinning a team or CI default without per-session `/model` picking. A manual `/model` pick still overrides it and persists across restarts.
- **Long-session memory:** v2.1.238 fixed unbounded memory growth in long interactive sessions — subagent tool results are now released once they scroll off the recent display window. Reduces the need to manually trim context in long-running sessions.
- **MCP / plugin security:** plugin marketplaces can now run a `headersHelper` command that mints short-lived tokens for catalog/archive fetches, rather than embedding long-lived credentials in config. Prefer this pattern when authoring or auditing a plugin marketplace.
- **v2.1.263** (2026-09-06): bug-fix/reliability release, no user-facing prompting changes. v2.1.261 (2026-09-04) also shipped a long tail of fixes worth knowing about if you hit them: a safer `rm -rf` guard that now catches positional-parameter and quoted-script forms, a fix for `claude -p --resume` adopting malformed session IDs, and an "Organization policy" line in `/status`/`claude doctor` explaining why an org policy failed to load.


## Cowork

File and task automation, now folded into the single Claude interface as of 2026-09-16 (see callout below) rather than a separate tab — still the right mental model for requests that end in a deliverable file, a scheduled routine, or work spanning multiple connected apps.

Major change — 2026-09-16, "Cowork and chat are now one Claude"

Anthropic merged Claude Cowork and Claude Chat into a single unified interface, and Claude Design now also works inline inside any conversation rather than only as its own surface. Claude "increasingly decide\[s\] the distinction on its own, depending on the user's prompt and request" — there is no more manual tab/mode switch for the user to make; a conversation can start as a quick question and turn into multi-step agentic work in place. **Everything already in Cowork is preserved**: existing chats, projects, artifacts, connectors, and skills carry over unchanged — this routine's own scheduled-task/Artifact setup needs no migration. A per-session check-in preference is configurable: "ask before taking an action" (default) versus "check in only when something needs a closer look."

Alongside the merge, two new products launched in **beta**: **Claude Docs** (a collaborative text editor for co-writing with Claude in real time, with comments and revision tracking, exportable to Word/PDF) and **Claude Slides** (prompt-or-research-to-deck generation, presentable directly from Claude or exportable to PowerPoint/PDF). Both are new, separately-branded products — distinct from Claude for Excel/PowerPoint (the desktop add-ins) and, as far as could be confirmed from launch coverage alone, appear to be a different (likely more capable/collaborative) surface from the existing "Pages"/"Slides" ready-made artifact types Cowork could already create from a type; it was not possible to confirm from public coverage whether the artifact-type flow now routes into Claude Docs/Slides under the hood or the two remain parallel — worth checking directly in-session next time a doc/deck request comes up, and re-verifying in a future run once documentation catches up.

**Rollout:** begins on Claude Pro and Max plans (web, desktop, mobile) "over the coming weeks"; Team and Free plans follow after that. Enterprise admins get a toggle for Docs/Slides beta enablement. **Prompting/scoping implication:** the §2/§4 "state the deliverable's format up front" guidance now matters even more than before, since a clear artifact request is what triggers the right (agentic vs. conversational) handling automatically rather than the user picking a surface — vague requests are more likely to get misrouted (answered conversationally when a file was wanted, or vice versa) than under the old two-tab model. Source: [claude.com/blog/cowork-is-now-claude](https://claude.com/blog/cowork-is-now-claude); corroborated by [TechCrunch](https://techcrunch.com/2026/09/16/anthropic-merges-claude-chat-and-cowork-in-one-interface/), [VentureBeat](https://venturebeat.com/technology/anthropic-is-killing-off-cowork-and-folding-it-into-claude-launching-claude-docs-and-claude-slides), and [Fortune](https://fortune.com/2026/09/16/anthropic-merges-its-claude-chat-and-agentic-cowork-products-into-a-single-ai-assistant-as-part-of-a-push-to-build-an-ai-superapp/) — all dated/checked 2026-09-16/17.

- **Surface-choosing rule of thumb (official framing, still useful post-merge as a way to think about capability even though there's no longer a separate tab):** if the request's success condition is "I have a file/artifact I can open, share, or keep," it needs agentic/Cowork-style capability. If it's "I have an answer," conversational handling is enough. If it's "the codebase changed," that's Code.
- State the deliverable's format up front (doc, sheet, deck, persisted page) — this determines which output-format skill gets invoked, changes how much structure the prompt needs, and (per the callout above) is now also what triggers correct auto-routing in the unified interface.
- For anything recurring, say so explicitly ("do this every Monday") rather than asking for a one-off — the scheduled-task tooling carried over from Cowork is built for this and produces a materially different, more maintainable setup than a one-shot prompt repeated by hand.
- When a task needs the user's own connected data (mail, drive, chat tools), name the specific source rather than a generic "check my stuff" — this avoids an unnecessary connector search and a slower first turn.

Desktop app changes — claude.com/docs/cowork/changelog, 2026-09-02 to 09-05

The Cowork/Claude desktop app changelog is tracked separately from the Claude Code CLI changelog in §3 and hadn't been checked in earlier runs of this document. Four items from the last two weekly cycles are worth folding into how you scope a Cowork request:

- **Device-bridge folder access widened** (v1.46388.3, 09-04): a user can now attach their whole home folder, Windows Documents/AppData, the macOS Library folder, or an entire drive — not just individual project folders. Claude's own config/session data, and credential or shell-startup locations (SSH keys, AWS/Google Cloud credentials, bash/zsh/PowerShell profiles), stay permanently off-limits regardless. Prompting implication: for a task spanning many folders, it's now reasonable to ask the user to attach one broad root instead of enumerating every subfolder.
- **Scheduled tasks now auto-retry** (v1.46388.1, 09-04): a failed scheduled-task run gets an automatic re-run rather than silently failing once and going quiet. Reinforces the existing recommendation above to use Cowork's native scheduled-task tooling for recurring work rather than a manual "remind me and I'll re-run it by hand" pattern.
- **Code sessions inside the desktop app** (v1.46388.1, 09-04) gained message queueing across the 5-hour usage-limit window — a message sent while limited now sends once the limit resets instead of being dropped — plus a keep-awake option for long-running sessions.
- **Broader entry point** (v1.44121.1, 09-02): Claude was added to the OS "Open with" menu for common work files on macOS/Windows, and default artifact-sharing visibility now follows organization-level settings instead of a fixed default — worth checking with enterprise users before assuming an artifact you publish is private by default.

Rolling infra change, since ~2026-09-10 — Cowork scheduled tasks and local projects migrating to run in the cloud

For accounts where Anthropic has started this rollout (not yet everyone), Cowork is moving local projects and locally-run scheduled tasks off the user's own computer and onto claude.ai's cloud infrastructure — the same execution model this very routine already uses. v1.52386.0 (2026-09-10) began the migration; v1.52386.3 (2026-09-11) fixed two rough edges: a migrating project no longer refuses new tasks for hours while its memory copy finishes server-side (it now accepts tasks immediately and finishes the copy in the background), and a scheduled task's auto-move now waits ~1 minute after the computer wakes and retries every minute while offline, instead of failing once and going quiet for hours.

**Prompting/scoping implication:** for an affected account, a "local device" task (one that needs `device_bash`/file-bridge access) may now actually be running in the cloud without local access — if a task that used to reach the user's files stops being able to, check whether that project has been migrated before assuming a permissions or connection problem. This also makes the Windows file-access bug below moot for any task that has already moved to the cloud on that account.

Resolved — 2026-09-14, Windows update fixes Cowork's on-device file access

The Windows-side bug that broke a locally-run Cowork session's access to the user's files (tracked below in earlier runs of this doc, ongoing since 2026-09-08/10) is now fixed. Microsoft shipped a follow-up Windows update, **KB5129195** (Windows 11 24H2 and 25H2), that corrects it — no Claude/Cowork update is needed on top of it. The fix is entirely on the affected user's end: they need to install the latest Windows update and restart. Prompting/scoping implication: if a Windows user still reports this symptom (Cowork failing to start or losing file access on their own machine), the first check is now whether they've installed KB5129195 and restarted, not whether their account's projects have migrated to the cloud. The original issue writeup is kept below for reference in case a similar regression recurs.

Historical — resolved 2026-09-14, see callout above

A Windows update released 2026-09-08 stopped a locally-run Cowork session from reaching the user's files on affected Windows PCs — tasks failed or the workspace never started. It affected Cowork running on third-party inference deployments and any Cowork session that ran on the user's own computer rather than in the cloud; cloud sessions (like this routine's own runs), Claude Code, and Chat were unaffected throughout. As of v1.52386.0 (2026-09-10) the app at least named the Windows update as the cause instead of pointlessly deleting and reinstalling the workspace, and as of 2026-09-14 Microsoft's own follow-up update resolved it outright — see the resolved callout above.

Added — v1.52386.0, 2026-09-10, Cowork/Code desktop app

Two changes worth knowing when scoping a session for a user: **message queueing** — a message sent while Claude is still replying no longer has to wait for a free moment; it queues and shows in the conversation, with a "Send now" option that interrupts the current turn to send it immediately (or Cmd/Ctrl+Enter to send-and-interrupt directly) — useful to tell a user about if they're used to a message getting lost or ignored when sent mid-reply. And a **default transcript view setting** for Claude Code sessions (Normal / Thinking / Verbose), settable under Settings → Claude Code or from a session's own Transcript view menu, syncing between the desktop app and claude.ai — worth setting to Verbose or Thinking by default for a user who wants to see an agent's intermediate reasoning without asking each session individually. Source: claude.com/docs/cowork/changelog, entry dated 2026-09-10.

Added — v2.110.0, 2026-09-15, Claude desktop app (note the versioning jump from the v1.52386.x series)

The desktop app's version numbering jumped from the v1.52386.x series straight to v2.110.0 with this release — not itself a prompting change, but worth knowing so a version-number comparison in a future run isn't misread as skipping hundreds of releases. Substantively, three groups of changes matter for scoping a session: **Cowork** fixed permission-prompt denials inside Dispatch, connected-folder issues (file deletion and folder-naming bugs), **Artifact tool read/update failures** (the exact mechanism this document is maintained through), and Windows startup problems including SSO reconnection; **Code** sessions now persist SSH/WSL connections through idle periods, fixed session-disconnection messages not being delivered, fixed permission-request display in split panes, and fixed handling of a large unsent prompt; and the **3P/enterprise** config surface gained several new options worth knowing about when scoping automation for an org-managed deployment — `scheduledTasksEnabled`, effort-level controls, and model-catalog support, alongside deployment-specific additions (`inferenceCredentialHelperArgs`, `inferenceFoundryBaseUrl`, an OIDC `redirectHost`) and changed Chat file-approval / MCP tool-permission handling.

Known issue, confirmed first-hand 2026-09-09 — GitHub connector and Cowork sandbox both refuse writes to GitHub

Tried, in one session: connecting GitHub as a custom remote MCP connector (`https://api.githubcopilot.com/mcp/`) authenticates fine and every read works (`get_me`, `get_file_contents`, `search_repositories`, …), but **every write call fails with `403 Resource not accessible by integration`** — `create_or_update_file`, `push_files`, and issue-write tools alike. This matches two open, unresolved public reports, not a one-off account issue: [anthropics/claude-ai-mcp#822](https://github.com/anthropics/claude-ai-mcp/issues/822) and [anthropics/claude-code#80874](https://github.com/anthropics/claude-code/issues/80874). Disconnecting/reconnecting the connector, revoking and re-authorizing on GitHub's side, and widening a fine-grained PAT's scopes do not fix it — the token the connector actually receives just isn't granted write capability, and the connector UI has no field to substitute a PAT directly.

The obvious fallbacks from inside a Cowork cloud sandbox are blocked too, deliberately: a plain `git clone` over Bash is refused by the session's own action classifier (even with no credentials involved), and a direct GitHub REST API write (`PUT .../repos/{o}/{r}/contents/{path}`) over HTTPS with a manually-supplied PAT is blocked at the network-egress-proxy layer with an explicit `"Write access to this GitHub API path is not permitted through this proxy"` — which itself points at `docs.anthropic.com/en/docs/claude-code/github-actions`. Reads over the same proxy (GET requests, `git clone` of public repos via raw `curl`) succeed, so this is a deliberate write-path restriction, not a general network block.

**Net effect:** as of this date, there is no working way for a Cowork session or scheduled task to write to a user's GitHub repo through the standard connector — not via the official connector as configured by default, not via raw git/API calls from inside the sandbox. Don't spend time re-debugging PAT scopes or connector permissions when this exact error shows up — it isn't a configuration problem on the user's side. See §7 for the fully-supported path (GitHub Actions).

**Update, 2026-09-13:** issue [\#822](https://github.com/anthropics/claude-ai-mcp/issues/822) is still open with no official fix, but several commenters report a partial workaround: instead of relying on the connector's own auth, manually install the [Claude GitHub App](https://github.com/apps/claude) directly on the target repo (github.com/apps/claude → Install → grant the specific repo). Reports are mixed — some get working writes afterward, others still hit "you don't have access to organization settings" during setup or find the connector and the manually-installed App are tracked as separate integrations with different grants, so it doesn't reliably fix things. Treat it as worth a quick try for a user blocked on this, not as a confirmed fix — re-verify write access with a real commit before relying on it, and fall back to §7's GitHub Actions path if it doesn't work.

**Update, 2026-09-14:** still no official fix on \#822, but the thread narrowed down what the "you don't have access to organization settings" error actually means: it shows up specifically when installing the Claude GitHub App on an *organization-owned* repo, and looks like a GitHub-side org-admin permission requirement (installing any GitHub App on an org repo needs an org owner/admin to approve it) rather than an Anthropic plan gate — a related report, [anthropics/claude-code#18467](https://github.com/anthropics/claude-code/issues/18467), has more detail. For a personal-account repo this shouldn't come up. Either way, verify the install actually took by checking `github.com/settings/installations` for the org/account in question — the Claude GitHub App should be listed there with the target repo granted — rather than assuming success from the setup flow alone; note the connector itself may still separately show as "not installed on any accounts" even once the App-based write path is working, since they're tracked separately.


## Claude Design

Multi-artboard visual design published as an editable canvas — UI mockups, landing pages, marketing graphics, print pieces.

- Anchor the prompt in one concrete subject, its audience, and the artboard's single job — vague briefs ("make it look nice") reliably regress to templated, generic layouts.
- State a treatment, not just a format: "utilitarian and information-dense" versus "editorial, one bold risk" changes typography, color, and layout decisions throughout, not just decoration.
- Name real constraints (brand colors, existing type system, must work in both light and dark) up front — Design applies an existing system when one is described and only invents one when none exists.
- For iteration, refer to elements by what they are ("the hero headline," "the pricing table") rather than by position — canvas elements move during refinement, so positional references go stale fast.


## Model-specific notes

Notes on how current-generation models (Claude Opus 5, Claude Sonnet 5, Claude Fable 5.1, and their predecessors still in rotation) respond differently to older prompting patterns.

- **Prefill** (seeding the start of the assistant turn to force a format) is a shrinking technique on current-generation models called via the API directly — explicit `<output_format>` instructions now get comparable compliance without it. Keep prefill for the narrow cases where only a hard format lock will do.
- **Verification instructions** ("double check your work," "explain your reasoning first") land differently on higher-effort/extended-thinking configurations, which already verify internally — an explicit instruction is more useful for lower-effort or non-reasoning configurations, where it's doing real work rather than being redundant.
- **Subagent delegation** patterns (Claude Code's `Agent`/Task tool, workflow scripts) are markedly more reliable on current models at following a self-contained brief with no shared memory of the parent conversation — lean on this rather than trying to carry context across agent boundaries.
- When the user names a specific model in a request, match your prompting pattern to it rather than defaulting to whatever pattern worked for the model you're most familiar with — capability and default verbosity shift release to release.

### Claude Fable 5.1 (and Mythos 5.1) — correct for these defaults

Official guidance — platform.claude.com, updated for the 2026-09-01 5.1 refresh

Fable 5.1 sits above Opus 5 in Anthropic's lineup (see §8) and is prompted differently enough from Sonnet/Opus that Anthropic publishes a dedicated guide. The seven corrections below are the ones worth applying by default; treat them as defaults to override, not rules to explain to the user unless asked.

- **Effort is the primary control.** The `effort` parameter (`low`/`medium`/`high`/`xhigh`/`max`) matters more for Fable 5.1 than model choice does for cost/quality tradeoffs. Default is `high`; `low`/`medium` are often competitive with Opus/Sonnet at lower cost, and `xhigh`/`max` need a larger `max_tokens` buffer. Benchmark down from `high` rather than assuming more effort is always better.
- **Progress updates are hidden by default** — short inter-tool notes arrive as `thinking` blocks with `display:"omitted"`. Set `thinking.display:"updates"`, or add:

      Before you start, say in a line what you're about to do; brief updates while you work help the user follow along. Close with a short recap.

- **Agent loops issue one tool call per turn** by default instead of batching independent calls in parallel. Nudge with:

      First privately list what you need next; then request every item that doesn't depend on another's result in this one response.

- **Conversation history should be append-only.** For accounts created after 2026-08-31, thinking blocks are bound to their conversation context — editing earlier messages invalidates cached thinking. Use server-side compaction/context-editing instead of client-side summarization.
- **Denser prose, less formatting by default** — fewer paragraph breaks, less bold/headers/lists than Claude 5 defaulted to. If you want either behavior corrected:

      Mannered prose substitutes metaphor and flourish for direct statement. The fix is to say what you mean. When a literal phrase is available, use it.

      Use lists and bullet points when asked to, or when the content is multifaceted enough that they help with clarity.

- **May describe next steps instead of finishing long autonomous tasks.** For unattended/long-horizon work, add explicit autonomy framing:

      You are operating autonomously. The user is not watching in real time and cannot answer questions mid-task, so asking "Want me to…?" will block the work. For reversible actions that follow from the original request, proceed without asking.

      Before ending your turn, check your last paragraph. If it is a plan, analysis, or promise about work you have not done ("I'll…"), do that work now. End your turn only when the task is complete or you are blocked on input only the user can provide.

- **Subagent coordination:** have subagent tools return immediately rather than blocking the lead agent, pass results back in a later `user` message, and give the lead a separate explicit "wait" tool if it needs one — cuts wall-clock time without hurting quality.
- **Vision on dense images/charts:** provide a crop/zoom tool rather than relying on a single full-image pass — iterative cropping delivers most of the accuracy uplift on complex figures.


## API / Claude Developer Platform

Direct API and platform-level agent-building capabilities: Messages API, tool use, computer use, Skills API, Files API.

Reached GA — 2026-08-20

**Computer use, the Skills API, and the Files API** are now production-ready (previously beta). Notable additions:

- A **browser-use tool**: reads page structure alongside screenshots, so agents can target web elements reliably instead of relying on pixel coordinates. Prefer it over raw computer-use for web-specific tasks.
- **Multiple actions per model turn** for computer use — fewer round-trips, materially faster task completion (one case study reported claims-workflow time dropping from 32 to 13 minutes).
- Computer use now qualifies for **HIPAA-regulated workloads under a BAA**.
- Files API: automatic expiration, 5× higher rate limits, 1 TB storage per organization.

New beta — 2026-09-14, Messages API on-demand compaction (`compact-2026-09-04`)

A second compaction mode alongside the existing automatic threshold-based compaction: a top-level `compaction` parameter now lets you trigger summarization **on your own schedule** instead of waiting for a token threshold, as a separate request billed as its own `usage.iterations` entry that can run in the background while the conversation keeps going. Send `compaction:{"type":"summarize"}` (optionally with custom `instructions`, which fully replace the default summarization prompt) with the `compact-2026-09-04` beta header; the response returns one signed `compaction` content block (`stop_reason:"compaction"`) that must be placed first in your next request, replacing the messages it summarized — keeping the summarized messages in front of it returns a 400. Excluding recent turns from the summarization request implements a **keep-tail** pattern: those turns stay word-for-word, and on models with preserved thinking, their thinking blocks stay valid as long as they directly followed the summarized messages and the system prompt/tools haven't changed since. Supported on Fable 5/5.1, Mythos 5/5.1/Preview, Opus 4.6–5, and Sonnet 4.6/5, across the Claude API, Bedrock, Google Cloud, and Microsoft Foundry; can't be combined with `context_management.edits` on the same request.

**Prompting/agent-design implication:** for a long-running agent loop built directly on the Messages API, this is a cleaner alternative to hand-rolled summarization — fire it asynchronously between turns rather than blocking the loop on it, and reach for the keep-tail pattern specifically when preserving extended-thinking chains across a compaction point matters more than a fully condensed history. If system-prompt rules or tool availability need to stay active after compaction, restate them in a system message after the block — the summary itself doesn't carry that forward automatically. Source: platform.claude.com/docs/en/build-with-claude/compaction, entry dated 2026-09-14.

Added — 2026-09-10, Claude Managed Agents / ant CLI

Two additions for teams running agents on Claude's Managed Agents infrastructure (server-hosted agents with a managed sandbox, distinct from Claude Code/Cowork): a new **`auto` permission-policy option** lets the server itself evaluate each agent/MCP tool call and run, deny, or pause it for approval, rather than every call needing a pre-declared static rule or human-in-the-loop approval — `agent.tool_use`/`agent.mcp_tool_use` events now report the evaluation decision so the outcome is auditable. Alongside it, **`ant beta:sessions connect`** attaches a terminal directly to a live Managed Agents session: follow it in real time, send it messages, and approve/deny tool calls waiting on the new `auto` policy (a `--web` flag serves Claude Console's session viewer locally instead).

**Prompting implication:** when designing a permission policy for a Managed Agents deployment, `auto` is now a real middle option between "pre-approve everything" (brittle, has to anticipate every tool call) and "approve every call by hand" (doesn't scale) — write the evaluation criteria into the policy prompt/config rather than trying to enumerate allowed calls statically, and use `ant beta:sessions connect` for spot-checking or intervening on a run instead of building a separate monitoring integration.

Source: platform.claude.com/docs/en/release-notes/overview, entry dated 2026-09-10.

Added — 2026-09-03, Developer Platform v1.30.0

The **`ant` CLI** gained an **`ant apply`** command: create and update agents, environments, skills, memory stores, and deployments from files, with approval workflows and lockfile management for reproducible deployments — an infrastructure-as-code pattern for platform resources rather than clicking through a console.

Confirmed working path, 2026-09-09 — Claude Code GitHub Actions for scheduled repo writes

For "Claude commits to my repo on a schedule" — the thing that doesn't work today from claude.ai/Cowork (see §4 known issue) — the supported, working path is the **Claude Code GitHub Action** (`anthropics/claude-code-action`). It runs inside the target repo's own GitHub Actions, not inside a claude.ai/Cowork session, authenticated with an `ANTHROPIC_API_KEY` or `CLAUDE_CODE_OAUTH_TOKEN` stored as an *encrypted GitHub repo secret* — proper secrets hygiene, nothing pasted into a prompt or chat. It ships a documented "run on a schedule" cron mode built for exactly this kind of daily job, including a worked example (daily commit/issue summary at a fixed UTC time).

- **Setup, quick:** run the `claude` CLI locally inside a clone of the target repo, run `/install-github-app`, follow the prompts — it installs the Claude GitHub App, adds the secret, and opens a PR with the workflow file to merge.
- **Setup, manual (no local CLI needed):** install the [Claude GitHub App](https://github.com/apps/claude) on the repo from a browser, add the secret under repo Settings → Secrets and variables → Actions, then commit a workflow file (starting point: [examples/claude.yml](https://github.com/anthropics/claude-code-action/blob/main/examples/claude.yml)) into `.github/workflows/`.
- **Tradeoff to flag before recommending this:** it's a separate automation surface from a Cowork scheduled task — the job runs as its own Claude Code session inside GitHub's runners and has no access to a claude.ai Artifact (Actions has no way to authenticate to a user's Claude account). So a repo file, not an Artifact, has to be the source of truth for whatever this writes; keeping both in sync requires deliberate design, not something to assume comes for free.

Source: code.claude.com/docs/en/github-actions — checked 2026-09-09.

### Prompting implications

- Package a repeated procedure as a versioned **skill** (instructions + scripts + templates) rather than re-explaining it in every prompt — this is now a first-class, production pattern, not a beta workaround.
- For browser/web-agent tasks, prompt for goals and let the browser-use tool handle element targeting, rather than hand-specifying pixel coordinates or brittle selectors.
- Since multiple computer-use actions can execute per turn, batch a task's actions into one instruction ("open settings, toggle X, save") rather than one action per prompt turn — it now maps to how the tool actually executes.
- For teams managing several agents/skills/deployments on the Developer Platform, prefer defining them as files and applying with `ant apply` over ad hoc console changes — it's now the reproducible, reviewable path, similar to infra-as-code.
- The `effort` parameter (`low`–`max`) is now the primary cost/quality lever on Fable 5.1 specifically — see §6 before assuming Opus/Sonnet's cost-control patterns (mostly model choice) transfer directly.
- When a task needs Claude to write to an external system on a recurring schedule (not just this session), check whether that system has its own scheduled/CI-native Claude integration (like GitHub Actions here) before defaulting to "give the Cowork scheduled task a stored credential" — a platform-native secrets store beats a token embedded in a prompt whenever one is available.


## New & emerging products

Filled in as Anthropic ships new Claude surfaces or model tiers that need their own prompting guidance.

New products, beta — Claude Docs & Claude Slides, launched 2026-09-16

Launched the same day as the Cowork/Chat merge (full details and prompting implications in §4's lead callout): **Claude Docs**, a real-time collaborative document editor, and **Claude Slides**, a presentation builder that drafts a deck from a prompt or research and can be presented directly from Claude or exported to PowerPoint/PDF. Both are in beta on Pro/Max plans, rolling out further over the coming weeks. See §4 for the full writeup, including the open question of how these relate to the existing Pages/Slides ready-made artifact types.

New model tier — Claude Fable 5.1 / Mythos 5.1, announced 2026-09-01

**Fable** and **Mythos** are a "Mythos-class" tier that sits above Claude Opus 5, first introduced as Fable 5 / Mythos 5 in June 2026 and refreshed to **5.1** on 2026-09-01. Both versions are the same underlying model at different safeguard levels: **Fable 5.1** (API id `claude-fable-5-1`) is generally available to everyone; **Mythos 5.1** has safeguards relaxed for vetted cyberdefense and life-sciences researchers through a trusted-access program and is not reachable through the standard API.

- Positioned for demanding reasoning and long-horizon agentic work — coding, research, computational biology — where Opus 5 evaluations still fall short. Anthropic's own recommendation: start with Opus 5 for most workloads, reach for Fable 5.1 when a task needs more headroom.
- 1M-token context, 128K max output; \$10/\$50 per million input/output tokens, cache reads at \$0.25/MTok (a 75% discount) — roughly 25–45% cheaper per typical-to-highly-agentic workload than the original Fable 5.
- Selectable today as a subagent model in Claude Code's/Cowork's `Agent` tool (alongside sonnet/opus/haiku). It needs several explicit corrections to its default behavior to prompt well — see §6 for the full list and copy-paste fixes.

**Claude Academy** (academy.claude.com, launched 2026-08-20) remains a free courses-and-tutorials learning hub — a resource to point users toward, not a promptable surface, so it doesn't get its own dedicated section here.

New vertical product — Claude for Financial Advisors, launched 2026-09-14

An Enterprise-tier Cowork plugin (installed from the Cowork plugin browser) purpose-built for financial advisors, rather than a general Claude surface — the first of what looks like a pattern of industry-specific plugin bundles worth watching for in future runs. It pairs a set of pre-built **workflow skills** — meeting prep and post-meeting summaries, portfolio rebalancing/drift review, alternative-investment briefs, estate/tax plan review, prospect intake, and compliance flagging against the SEC Marketing Rule — with connectors into the tools advisors already use: custody/portfolio (Charles Schwab, BlackRock, Vanguard), alternatives (iCapital, Addepar), wealth-management platforms (Envestnet, Orion, SS&C Black Diamond), and CRM/client-data tools (Wealthbox, Zocks, Wealth.com), on top of the existing Microsoft 365/Salesforce/DocuSign/Box/FactSet/S&P Global/Morningstar connectors.

**Prompting/scoping implication:** all critical outputs (investment recommendations, compliance determinations) are designed to require advisor sign-off rather than acting autonomously — when helping a user prompt this plugin, frame requests as producing a draft/brief/flag for the advisor to review, not as an instruction to execute a financial or compliance decision directly. Pricing is enterprise-license-only; licenses requested before end of September 2026 get a one-time usage credit. Sources: [unite.ai](https://www.unite.ai/anthropic-launches-claude-for-financial-advisors-with-partner-connectors/), [wealthmanagement.com](https://www.wealthmanagement.com/artificial-intelligence/anthropic-launches-claude-for-financial-advisors) — checked 2026-09-15.


## Community & open-source guidance — where it diverges

Reliable community prompting resources are useful, but a few widely-repeated patterns lag current official guidance.

Divergence

Community guides (e.g. promptingguide.ai and many GitHub prompt-pattern collections) still lead with heavy prefill and "act as X" role-play framing as default techniques. Official current guidance treats explicit `<output_format>`/role instructions as sufficient for most cases and reserves prefill for hard format locks — see §6.

Divergence

Some community agent-framework guides recommend maximal upfront context-stuffing ("give the agent everything it might need"). Official Claude Code guidance instead favors a lean `CLAUDE.md` plus on-demand file reads and subagent delegation — large unconditioned context tends to dilute attention rather than help.


## Changelog

- 2026-09-17

  **Push-notification-worthy: yes.** The largest single item since this document's rebuild: on 2026-09-16 Anthropic **merged Claude Cowork and Claude Chat into one unified Claude interface** and put **Claude Docs** and **Claude Slides** into beta. §4 rewritten with a lead callout: no more manual tab/mode switching — Claude now decides on its own, from the prompt, whether a request needs conversational or agentic/Cowork-style handling; existing Cowork chats, projects, artifacts, connectors and skills all carry over unchanged (this routine's own scheduled-task + Artifact setup needs no action); a per-session check-in preference (ask-before-each-action vs. check-in-only-when-needed) is now configurable; Claude Design also now works inline in any conversation instead of only as its own surface. Claude Docs is a real-time collaborative text editor (comments, revision tracking, Word/PDF export); Claude Slides is prompt/research-to-deck generation (presentable directly from Claude, PowerPoint/PDF export) — both beta, rolling out to Pro/Max first (web/desktop/mobile) with Team/Free to follow, Enterprise admins get an enablement toggle. Flagged as an open question for a future run: whether Claude Docs/Slides supersede, replace, or run parallel to the existing Pages/Slides ready-made artifact types Cowork could already create from a type — launch coverage didn't clarify this and it's worth checking directly next time a doc/deck request comes up. Updated §0's "identify which product" framing and §2's Chat section to reflect that stating the deliverable clearly is now what triggers correct auto-routing, since there's no longer a manual surface pick to fall back on. §3: added Claude Code v2.1.274 (2026-09-17, a very large release) — a new agent-map view of a session's sub-agents, a fix for sessions getting stuck in endless "unexpected tool_use_id" error loops, and (relevant to the tracked GitHub-connector-write-403 issue) Claude Code web "routines" now skip-and-retry for 72 hours instead of failing outright when the owner's GitHub connection is missing. Checked and found unchanged/not material: no new anthropic.com/news posts (the merge announcement itself was published on claude.com/blog, not anthropic.com/news); no new Claude Developer Platform release-notes entries beyond the already-recorded 2026-09-14 compaction beta; no Cowork/desktop-app-changelog entries beyond the already-recorded v2.110.0 (2026-09-15) — the merge announcement is a product/blog event, not yet reflected as its own changelog line item as of this check; no movement on the GitHub-connector-write-403 issue ([\#822](https://github.com/anthropics/claude-ai-mcp/issues/822)) beyond what's already recorded; no corroborated new base model beyond the already-recorded Fable/Mythos 5.1. A push notification was sent this run given the scale and direct relevance of the Cowork/Chat merge to how this very account uses Claude. Sources: [claude.com/blog/cowork-is-now-claude](https://claude.com/blog/cowork-is-now-claude); [TechCrunch](https://techcrunch.com/2026/09/16/anthropic-merges-claude-chat-and-cowork-in-one-interface/); [VentureBeat](https://venturebeat.com/technology/anthropic-is-killing-off-cowork-and-folding-it-into-claude-launching-claude-docs-and-claude-slides); [Fortune](https://fortune.com/2026/09/16/anthropic-merges-its-claude-chat-and-agentic-cowork-products-into-a-single-ai-assistant-as-part-of-a-push-to-build-an-ai-superapp/); code.claude.com/docs/en/changelog; claude.com/docs/cowork/changelog; platform.claude.com/docs/en/release-notes/overview; anthropic.com/news — all checked 2026-09-17.

- 2026-09-16

  Routine research run: three genuinely new items folded in, none judged large enough for a push notification. §7: added the Messages API's new on-demand compaction beta (`compact-2026-09-04`, platform release notes dated 2026-09-14) — a top-level `compaction` parameter that lets an agent trigger summarization on its own schedule (optionally in the background) instead of waiting for automatic threshold compaction, with a keep-tail pattern for preserving recent turns and their thinking chains word-for-word; new callout with the mechanics and agent-design implications. §3: added Claude Code v2.1.273 (2026-09-15) — a bug-fix release, but with two items worth knowing (a fixed false-failure report for subagents/background agents whose final streamed reply omitted token usage or a model id, and a fixed false-refusal by the safety classifier of large-output-schema `Workflow` `agent()` calls) plus new LLM-gateway request headers. §4: added a callout for Claude desktop v2.110.0 (2026-09-15) — notable for a versioning-scheme jump from the v1.52386.x series, fixes to Artifact read/update failures and connected-folder issues in Cowork (both relevant to this routine's own maintenance mechanism), and new enterprise/3P config options (`scheduledTasksEnabled`, effort controls, model-catalog support) worth knowing when scoping automation for an org-managed deployment. Checked and deliberately **not** added: a KuCoin-syndicated claim (crediting AIBase/a MetaEra original, dated 2026-09-15) that Anthropic expanded "Claude for Small Business" to 43 workflows and 27 app integrations — the actual anthropic.com/news post by that name is dated 2026-05-13 (15 workflows, 7 integrations) and anthropic.com/news shows no September follow-up, so this is treated the same way this document treats other uncorroborated blog claims (e.g. the recurring "Opus 5.1"/"Sonnet 5.1" rumors) — not added without a primary-source citation. Also checked and found unchanged/not material: no further Claude Developer Platform release-notes entries beyond the 2026-09-14 compaction beta; no Claude Design, Claude in Chrome, or Claude in Excel updates found; the GitHub-connector-write-403 issue ([\#822](https://github.com/anthropics/claude-ai-mcp/issues/822)) shows no new dated developments beyond what's already recorded from 2026-09-13/14; no new anthropic.com/news posts since the already-recorded 2026-09-10 threat-intelligence report; no visible change to the official prompt-engineering overview docs. Nothing today reverses existing guidance or introduces a new product/model, so no push notification was sent this run. Sources: code.claude.com/docs/en/changelog; claude.com/docs/cowork/changelog; platform.claude.com/docs/en/release-notes/overview; platform.claude.com/docs/en/build-with-claude/compaction; anthropic.com/news; kucoin.com (checked, not used as a source); github.com/anthropics/claude-ai-mcp/issues/822 — all checked 2026-09-16.

- 2026-09-15

  Routine research run: several genuinely new items found and folded in — largest single update since the 2026-09-06 rebuild, but still judged not individually notification-worthy on its own (see reasoning below). §3: added Claude Code v2.1.271 (2026-09-14, a large release) — per-command `allowed_domains` for Bash/PowerShell/Monitor in auto mode (network access scoped to what one command needs rather than the whole session), `omitClaudeMd` agent frontmatter for a subagent that should skip the parent project's CLAUDE.md files, fast mode reaching Remote sessions, Monitor watches now always carrying a deadline instead of running indefinitely (re-arm required), dynamic workflows pausing/auto-resuming across a usage-limit reset, and the Pro-plan dynamic-workflow default/medium size shrinking — and v2.1.272 (2026-09-15, fixes only, nothing prompting-relevant). §4: marked the Windows Cowork file-access known issue (tracked here since 2026-09-10) **resolved** — Microsoft's KB5129195 update fixes it on the user's end, no Claude update needed; kept the original writeup below the new resolved callout for reference. Also added a callout for two v1.52386.0 (2026-09-10) desktop-app items that earlier runs hadn't recorded: message queueing with a "Send now"/interrupt option for a message sent mid-reply, and a default transcript view setting (Normal/Thinking/Verbose) for Claude Code sessions, syncing between desktop and claude.ai. §8: added **Claude for Financial Advisors** (launched 2026-09-14) — an Enterprise Cowork plugin bundling financial-industry workflow skills (meeting prep, portfolio rebalancing review, compliance flagging, estate/tax review, prospect intake) with connectors to custody/portfolio, wealth-management, and CRM platforms advisors already use; noted the advisor-sign-off-required framing for prompting it. Considered for a push notification and decided against one: the new product is enterprise/vertical-specific (not something this account's typical usage touches), the CLI release is incremental within an already-tracked cadence, and the Windows fix is a "problem going away" rather than a new thing to act on — none of it meets the bar of something the user needs to see now rather than find here next time they open this doc. Checked and found unchanged/not material: no new Cowork/desktop release beyond v1.52386.6 (2026-09-13, already superseded by the Windows-fix news above); no new Claude Developer Platform release-notes entries beyond the already-recorded 2026-09-10 Managed Agents item; no new anthropic.com/news posts of prompting/product relevance beyond the already-recorded Financial Advisors launch; no change to Anthropic's official prompt-engineering overview docs; no Claude Design, Claude in Chrome, or Claude in Excel updates found; no corroborated new base model beyond the already-recorded Fable/Mythos 5.1. Also noted but deliberately left out, consistent with the 2026-09-14 run's scoping decision: the permanent Claude Code weekly-limit change (25% over pre-promo baseline, replacing the expired 50% promo) is a quota/billing matter, not a prompting technique or product change, so it stays outside this document's scope. Sources: code.claude.com/docs/en/changelog; claude.com/docs/cowork/changelog; platform.claude.com/docs/en/release-notes/overview; anthropic.com/news; unite.ai; wealthmanagement.com — all checked 2026-09-15.

- 2026-09-14

  Routine research run: nothing large enough for a push notification. §4: refined the GitHub-connector-write-403 known issue with a small but genuinely useful addition — the "you don't have access to organization settings" error some users hit when trying the manual-GitHub-App-install workaround appears to be a GitHub-side org-admin permission requirement for installing an App on an *organization-owned* repo (see [anthropics/claude-code#18467](https://github.com/anthropics/claude-code/issues/18467)), not an Anthropic plan gate, and shouldn't come up on a personal-account repo — plus a concrete way to verify the install actually took (`github.com/settings/installations`). Considered and deliberately excluded as out of scope for this doc: Anthropic's Claude Code weekly-limit change taking effect today (permanent 25% increase over the pre-promotion baseline replacing the temporary 50% boost that expired 2026-09-13, a net ~17% reduction from yesterday's level, per Anthropic's own follow-up post) — this was announced publicly around 2026-08-30, so it's schedule, not new guidance, and it's a quota/billing change rather than a prompting technique, new product, or design workflow, which is this document's stated scope; noting the exclusion here in case a future run needs to reconsider that boundary. Checked and found unchanged/not material: no Claude Code release beyond v2.1.270 (2026-09-12); no new Cowork/desktop-app release beyond v1.52386.3 (2026-09-11); no new Claude Developer Platform release-notes entries beyond the already-recorded 2026-09-10 Managed Agents item; no new anthropic.com/news posts since 2026-09-10; no visible change to Anthropic's official prompt-engineering overview/best-practices docs; no Claude Design, Claude in Chrome, or Claude in Excel updates found; no corroborated new base model — "Opus 5.1"/"Sonnet 5.1" rumors continue circulating on low-quality blogs with zero corroboration on anthropic.com/news or platform.claude.com, so still not added, per the standard applied on every prior run. Sources: code.claude.com/docs/en/changelog; claude.com/docs/cowork/changelog; platform.claude.com/docs/en/release-notes/overview; anthropic.com/news; platform.claude.com/docs/en/build-with-claude/prompt-engineering/overview; github.com/anthropics/claude-ai-mcp/issues/822; github.com/anthropics/claude-code/issues/18467; bleepingcomputer.com (Claude Code limits change) — all checked 2026-09-14.

- 2026-09-13

  Routine research run: nothing large enough for a push notification, but three items worth recording. §4: a new official callout on a rolling infra change — Cowork is migrating local projects and locally-run scheduled tasks to run in the cloud (claude.ai) for accounts where Anthropic has started the rollout (v1.52386.0, 2026-09-10; refined v1.52386.3, 2026-09-11) — noted the scoping implication that an affected account's "local device" task may quietly be running cloud-side now, and that this makes the existing Windows file-access known issue moot once a project has migrated. Updated that Windows known-issue callout: v1.52386.0 improved only the error message (names the Windows update as the cause) — Anthropic still has not shipped an actual fix as of today. Updated the GitHub-connector-write-403 known issue with a 2026-09-13 finding: issue \#822 remains open, but several commenters report a partial, inconsistent workaround (manually installing the Claude GitHub App directly on the repo rather than relying on the connector's own auth) — flagged as worth trying but not a confirmed fix. §3: added Claude Code v2.1.270 (2026-09-12), a single fix reverting a v2.1.269 regression that made read-only git commands unexpectedly ask for permission again in long-running sessions — relevant to unattended/scheduled sessions that assumed read-only git needs no approval. Checked and found unchanged/not material: no new Claude Code release beyond v2.1.270; no new Claude Developer Platform release-notes entries since the already-recorded 2026-09-10 Managed Agents item; no new Anthropic news/blog posts beyond continued coverage of the already-recorded 2026-09-10 threat-intelligence report; no official Claude Design update; and no corroborated new base model — "Opus 5.1"/"Sonnet 5.1" rumors continue to circulate on low-quality blogs and social media with zero corroboration on anthropic.com/news or platform.claude.com, so still not added, per the standard applied on every prior run. Sources: code.claude.com/docs/en/changelog; claude.com/docs/cowork/changelog; platform.claude.com/docs/en/release-notes/overview; anthropic.com/news; github.com/anthropics/claude-ai-mcp/issues/822 — all checked 2026-09-13.

- 2026-09-12

  Routine research run: three genuinely new items found and folded in, none individually large enough to warrant a push notification (consistent with the incremental-release standard applied on prior runs). §7: added Claude Developer Platform's 2026-09-10 Managed Agents release — a new `auto` permission-policy option letting the server evaluate/run/deny/pause each agent or MCP tool call itself (with the decision reported on `agent.tool_use`/`agent.mcp_tool_use` events), plus `ant beta:sessions connect` for attaching a terminal to a live Managed Agents session to follow it and approve/deny pending calls — a new callout with a prompting/policy-design implication. §4: added a new known-issue callout for a Windows update (2026-09-08, includes KB5124008) that breaks a locally-run Cowork session's access to the user's files on affected Windows PCs (cloud sessions, Claude Code, and Chat are unaffected; no fix yet, only Anthropic's diagnosis) — relevant for scoping a Cowork task with a Windows user. §3: added Claude Code v2.1.269 (2026-09-11) to the recently-changed list — mostly bug/cache fixes, but `/output-style [name]` (switch output styles from the command line, including headless/cloud/Remote Control), `claude plugin eval` (scored, reproducible plugin/skill eval reports), and a raised Workflow-tool concurrent-agent cap are worth knowing. Checked and found unchanged/not material since 09-11: Anthropic news/blog (only continued coverage of the already-recorded 09-10 threat-intelligence report — Bloomberg pieces on Iran/Russia/Yemen misuse cases, not prompting-relevant), Claude Design (no official update found), and no corroborated new base model beyond the already-recorded Fable/Mythos 5.1 — several low-quality blogs (CometAPI, CellCog) speculate about imminent "Opus 5.1"/"Sonnet 5.1" but nothing on anthropic.com/news or platform.claude.com confirms it, so still not added, per the same standard applied on every prior run. Sources: code.claude.com/docs/en/changelog; claude.com/docs/cowork/changelog; platform.claude.com/docs/en/release-notes/overview; anthropic.com/news — all checked 2026-09-12.

- 2026-09-11

  Routine research run: added Claude Code v2.1.268 (2026-09-10, a very large bug-fix/feature release) to §3. The one change worth flagging for this document's own guidance: task-tracking tools (`TaskCreate`/`TaskGet`/`TaskUpdate`/`TaskList`, `TodoWrite`) are now offered by default only on Claude 3.x and specific older Opus/Sonnet/Haiku versions — current-generation models (Opus 5, Sonnet 5, Fable 5.1) need `CLAUDE_CODE_ENABLE_TODO_TOOLS=1` set to keep getting them, which affects any prompt or skill that assumes task-list tooling is always present on a current model (this routine's own Cowork session is itself a current-model case that depends on these tools). Also added from the same release: browser-tab icons Claude now chooses for published artifacts (the Artifact tool's existing `icon` parameter going live end-to-end), a local Cowork session with all approvals skipped now refusing an Artifact-tool read of a local file outside the session's folders or behind a symlink instead of silently reading it, and plain `WebFetch` permission rules no longer covering Artifact tool reads/updates (a dedicated `Artifact` rule is needed). Checked and found unchanged since 09-10: Cowork/Claude-desktop-app changelog (still v1.49585.0, 09-08), Claude Developer Platform (still `ant apply` as the latest item, 09-03), Anthropic news/blog (only a September 10 threat-intelligence report on AI misuse, not prompting-relevant), Claude Design, and no corroborated new model beyond the already-recorded Fable/Mythos 5.1 (the "Opus 5.1"/"Sonnet 5.1" social-media rumor is still circulating with no corroboration on anthropic.com/news or platform.claude.com — still not added, same standard as prior runs). Nothing here rose to notification-worthy — a large but incremental CLI release, no new model/product/best-practice reversal — so no push notification was sent this run. Sources: code.claude.com/docs/en/changelog; claude.com/docs/cowork/changelog; releasebot.io/updates/anthropic/claude-developer-platform; anthropic.com/news — all checked 2026-09-11.

- 2026-09-10

  Routine research run: added Claude Code v2.1.267 (2026-09-09) and Cowork/Claude-desktop v1.49585.0 (2026-09-08, the first desktop release since 09-05) to §3. v2.1.267 is mostly bug fixes but extends the prompt-cache-reuse fix series already tracked here — it closes several more cache-miss cases beyond the two 2.1.265 fixed (model switches via `/model`, MCP tools changing across a session resume, tool-list rewrites on MCP reconnect or mid-session worktree additions, and subagents/sessions started with `--system-prompt`/`--append-system-prompt`) — plus a new `maxEffortLevel` setting (cross-referenced from §6), an `effort:` frontmatter fix for models with pinned default effort, and the Artifact tool now retrying a dropped-connection publish once and giving line/column detail on invalid-UTF-8 publish failures (relevant to this routine's own maintenance mechanism). v1.49585.0's one prompting-relevant item: Claude API/Vertex/Bedrock deployments with no custom base URL no longer need `toolSearchEnabled` — tool search is on by default there now. Checked and found unchanged since 09-09: no new Anthropic news/blog posts (anthropic.com/news), no Claude Design changes, no corroborated new model beyond the already-recorded Fable/Mythos 5.1 (an uncorroborated social-media claim of imminent "Opus 5.1"/"Sonnet 5.1" releases is circulating again — still nothing on anthropic.com/news or platform.claude.com confirming it, so still not added, per the same standard applied 2026-09-09), and no material change to official prompting-technique guidance beyond what's already in §1/§6. Nothing here rose to notification-worthy — no new model, product, or best-practice reversal, just incremental releases — so no push notification was sent this run. Sources: code.claude.com/docs/en/changelog; claude.com/docs/cowork/changelog; anthropic.com/news — all checked 2026-09-10.

- 2026-09-09

  Routine research run: added Claude Code v2.1.265 (2026-09-08, the largest release since the prior check) and its same-day v2.1.266 hotfix to §3. Most relevant to this document's existing guidance: v2.1.265 fixed two prompt-cache-reuse bugs that were undermining the subagent-delegation pattern §3/§6 already recommend (a resumed foreground-spawned subagent whose tool list/system prompt changed, and agent teammates/resumed subagents moving `SubagentStart` hook context and preloaded skills out of the prompt prefix) — worth knowing if you diagnosed cache misses on that pattern before this version. Also added: the 1 GB tool-result disk cap with truncation notice, `--plugin-dir` now accepting a folder of plugins, an MCP legacy HTTP+SSE transport fallback fix, non-interactive sessions now persisting `cd` across turns (relevant to headless/scripted automation), and the Artifact tool's read of an artifact someone else wrote now explicitly flagging embedded instructions as untrusted content rather than relaying them. v2.1.266 reverted a same-day regression where the undocumented `CLAUDE_CODE_USE_GATEWAY` env var started forcing Cloud-gateway sign-in and broke API-key/custom-auth-header setups. Checked and found unchanged since the last pass: Cowork/Claude-desktop-app changelog (still v1.46388.4, 09-05), Claude Developer Platform (still `ant apply` as the latest item, 09-03), Claude Chat/Design/Agent SDK/Chrome/Excel (no new items surfaced), and no new model announcements beyond the already-recorded Fable/Mythos 5.1 (some low-quality blogs speculate about "Opus 5.1"/"Sonnet 5.1," but nothing on anthropic.com/news or platform.claude.com corroborates this — treating as unverified rumor, not adding). Sources: code.claude.com/docs/en/changelog; claude.com/docs/cowork/changelog; releasebot.io/updates/anthropic/claude-developer-platform and /claude — all checked 2026-09-09.

- 2026-09-09

  First-hand finding, not routine research: the user asked to mirror this document to a personal GitHub repo with daily automated pushes. Confirmed two independent, currently-broken write paths and one working-but-heavier alternative, written up as new callouts in §4 and §7 for future reference. §4: connecting GitHub as a claude.ai custom MCP connector authenticates and reads fine but every write call returns `403 Resource not accessible by integration` — matches open reports [anthropics/claude-ai-mcp#822](https://github.com/anthropics/claude-ai-mcp/issues/822) and [anthropics/claude-code#80874](https://github.com/anthropics/claude-code/issues/80874), not fixable by reconnecting/re-authorizing/PAT scoping. Also confirmed the Cowork cloud sandbox independently blocks the fallbacks: plain `git clone` is refused by the session's action classifier, and a direct GitHub REST API write over HTTPS is blocked at the network-egress-proxy layer ("Write access to this GitHub API path is not permitted through this proxy"), which itself points at the GitHub Actions integration. §7: documented that working alternative — the Claude Code GitHub Action's cron/schedule mode, authenticated via an encrypted GitHub repo secret rather than a token in a prompt — including setup steps and the tradeoff that it can't reach a claude.ai Artifact directly. Net decision: no protocol change to this document's own daily maintenance (still Cowork scheduled task + Artifact, no GitHub push); did a one-time manual markdown/HTML export instead for the user to upload by hand. Re-check periodically whether issue \#822 has shipped a fix. Sources: first-hand testing this session; github.com/anthropics/claude-ai-mcp/issues/822; github.com/anthropics/claude-code/issues/80874; code.claude.com/docs/en/github-actions — 2026-09-09.

- 2026-09-07

  Added a new §4 callout on Cowork/Claude-desktop-app changes (v1.44121.1–v1.46388.4, 2026-09-02 to 09-05) that earlier runs of this document had missed, because they only checked the Claude Code CLI changelog (code.claude.com) and not the separate desktop-app changelog (claude.com/docs/cowork/changelog): device-bridge folder access widened to whole home folders/Documents/AppData/Library/entire drives (credential and shell-startup locations, plus Claude's own config, stay permanently excluded); scheduled tasks now auto-retry on failure; Code sessions inside the desktop app gained message queueing across the 5-hour usage limit plus a keep-awake option; and Claude was added to the OS "Open with" menu, with artifact-sharing defaults now following org-level settings. Checked and found unchanged since 09-06: Claude Code CLI (still v2.1.263), Claude Fable/Mythos 5.1 details, Claude Design, and API/Platform beyond the already-recorded `ant apply` release. Sources: claude.com/docs/cowork/changelog — checked 2026-09-07.

- 2026-09-06

  Added Claude Fable 5.1 and Claude Mythos 5.1 (announced 2026-09-01) as a new "Mythos-class" model tier above Opus 5 — new §8 entry covering GA status, restricted Mythos access, context/pricing, and its availability as a Claude Code/Cowork subagent model. Added a substantial new §6 subsection, "Claude Fable 5.1 — correct for these defaults," from Anthropic's official prompting guide: the `effort` parameter as the primary cost/quality control, hidden-by-default thinking updates, one-tool-call-per-turn agent loops, append-only conversation history for cached thinking, denser/less-formatted prose by default, a tendency to describe next steps instead of finishing long autonomous tasks, subagent coordination patterns, and vision/cropping guidance — each with a copy-pasteable fix snippet. Cross-referenced from §3 and §7. Sources: anthropic.com/claude-fable-and-mythos-5-1, anthropic.com/news/claude-fable-5-mythos-5, platform.claude.com/docs/.../prompting-claude-fable-5-1, platform.claude.com/docs/en/models/overview — checked 2026-09-06.

- 2026-09-06

  Added Claude Code v2.1.259–2.1.261/2.1.263 (2026-09-02 to 09-06) to §3: `/skill-doctor` for skill-usage/context-cost auditing, fullscreen `/diff` panel, prompt-cache-miss diagnostics in `/cost`/status line, `bashOutputMaxChars`/`taskOutputMaxChars` (up to 128K chars), `--append-subagent-system-prompt-file`, headless `/reload-plugins` and `--permission-prompts none`, and the `managedMcpServers` setting. Added Developer Platform v1.30.0's `ant apply` CLI command to §7 (agents/environments/skills/memory stores/deployments as files, with approval workflow and lockfiles). Source: code.claude.com/docs/en/changelog and corroborating changelog aggregators, checked 2026-09-06. Also fixed the underlying scheduled task: it previously ran research daily but was never wired to write findings back into this document — it now reads this artifact, updates it in place when something material changes, and republishes to the same URL every run.

- 2026-08-22

  Rebuilt this reference as a Claude.ai Artifact (previously a Google Doc). Google Drive read/write access was gated behind a permission prompt that kept timing out in the scheduled-task environment, so per the user's request this file now lives here and is updated in place by the same daily scheduled task. Content refreshed from official sources current as of this date: Claude Code v2.1.236–2.1.239 (`ANTHROPIC_DEFAULT_MODEL`, Concise output style, output-style drift fix, subagent memory-release fix, plugin `headersHelper`); Computer Use / Skills API / Files API reaching GA on the Claude Developer Platform; Claude Academy learning-hub launch (noted as a resource, §8).

Maintained by the daily "Claude & Claude Code best-practices watch" scheduled task. Re-checked each run against official docs (platform.claude.com, code.claude.com, claude.com/blog, support.claude.com) plus community prompting resources; only rewritten here when something material changes.